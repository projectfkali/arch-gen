# Contributing

Thanks for considering contributing.

## Development

```bash
npm install
npm run lint
npm test
npm run build
```

## Running the CLI locally

```bash
npm run dev -- generate -i examples/ecommerce-schema.json -o ./output
```

## Pull requests

- Keep changes focused and add/adjust tests when behavior changes.
- Ensure `npm run lint` and `npm test` pass.

